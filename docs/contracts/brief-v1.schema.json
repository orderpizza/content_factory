{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "x-maturity": "superseded",
  "x-replacement-owner": "docs/specs/idea-intake-and-determination.md",
  "$comment": "Retained unimplemented draft. Do not use for Phase 1 production; author a new version under the replacement owner before activation.",
  "$id": "content_factory/brief_v1",
  "title": "BriefRevision brief_json",
  "type": "object",
  "additionalProperties": false,
  "required": ["editorial_goal", "coverage_kind", "canonical_target", "audience", "desired_outcome", "constraints", "requested_changes"],
  "properties": {
    "editorial_goal": {"type": "string", "minLength": 1, "maxLength": 1200},
    "coverage_kind": {"enum": ["teaching_target", "trend_explainer", "other_approved"]},
    "canonical_target": {"type": "string", "minLength": 1, "maxLength": 300},
    "audience": {"type": "string", "minLength": 1, "maxLength": 500},
    "desired_outcome": {"type": "string", "minLength": 1, "maxLength": 500},
    "constraints": {"type": "array", "maxItems": 30, "items": {"type": "string", "minLength": 1, "maxLength": 500}},
    "requested_changes": {"type": "array", "maxItems": 30, "items": {"type": "string", "minLength": 1, "maxLength": 500}}
  },
  "x-owner": "docs/specs/idea-intake-and-determination.md"
}
